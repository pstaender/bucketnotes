# Router mode used by the app.
# Set to "HashRouter" to use hash-based routing (e.g. when serving the built
# app from a static host without SPA fallback/rewrite support).
# Any other value, or leaving this unset, uses path-based BrowserRouter.
VITE_APP_ROUTER=BrowserRouter
